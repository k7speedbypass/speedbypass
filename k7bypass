

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "K7SpeedBypass"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 350, 0, 250)
mainFrame.Position = UDim2.new(0.5, -175, 0.5, -125)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
mainFrame.BackgroundTransparency = 0.1
mainFrame.BorderSizePixel = 2
mainFrame.BorderColor3 = Color3.fromRGB(255, 85, 85)
mainFrame.ClipsDescendants = true
mainFrame.Parent = screenGui

-- Corner rounding
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = mainFrame

-- Title
local title = Instance.new("TextLabel")
title.Name = "Title"
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
title.BackgroundTransparency = 0.2
title.Text = "K7 SPEED BYPASS"
title.TextColor3 = Color3.fromRGB(255, 85, 85)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Center
title.Parent = mainFrame

-- Divider
local divider = Instance.new("Frame")
divider.Size = UDim2.new(1, 0, 0, 2)
divider.Position = UDim2.new(0, 0, 0, 40)
divider.BackgroundColor3 = Color3.fromRGB(255, 85, 85)
divider.BackgroundTransparency = 0.5
divider.Parent = mainFrame

-- Power Setting Label
local powerLabel = Instance.new("TextLabel")
powerLabel.Size = UDim2.new(1, 0, 0, 30)
powerLabel.Position = UDim2.new(0, 0, 0, 55)
powerLabel.BackgroundTransparency = 1
powerLabel.Text = "SET POWER (10k - 500k):"
powerLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
powerLabel.TextSize = 14
powerLabel.Font = Enum.Font.Gotham
powerLabel.TextXAlignment = Enum.TextXAlignment.Center
powerLabel.Parent = mainFrame

-- Power Input Box
local powerBox = Instance.new("TextBox")
powerBox.Size = UDim2.new(0, 200, 0, 35)
powerBox.Position = UDim2.new(0.5, -100, 0, 90)
powerBox.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
powerBox.BackgroundTransparency = 0.3
powerBox.Text = "100000"
powerBox.TextColor3 = Color3.fromRGB(255, 85, 85)
powerBox.TextSize = 16
powerBox.Font = Enum.Font.GothamBold
powerBox.TextXAlignment = Enum.TextXAlignment.Center
powerBox.ClearTextOnFocus = false
powerBox.Parent = mainFrame

local powerBoxCorner = Instance.new("UICorner")
powerBoxCorner.CornerRadius = UDim.new(0, 4)
powerBoxCorner.Parent = powerBox

-- Toggle Key Label
local toggleLabel = Instance.new("TextLabel")
toggleLabel.Size = UDim2.new(1, 0, 0, 30)
toggleLabel.Position = UDim2.new(0, 0, 0, 135)
toggleLabel.BackgroundTransparency = 1
toggleLabel.Text = "TOGGLE KEY:"
toggleLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
toggleLabel.TextSize = 14
toggleLabel.Font = Enum.Font.Gotham
toggleLabel.TextXAlignment = Enum.TextXAlignment.Center
toggleLabel.Parent = mainFrame

-- Key Display Button
local keyButton = Instance.new("TextButton")
keyButton.Size = UDim2.new(0, 60, 0, 35)
keyButton.Position = UDim2.new(0.5, -30, 0, 165)
keyButton.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
keyButton.BackgroundTransparency = 0.3
keyButton.Text = "R"
keyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
keyButton.TextSize = 18
keyButton.Font = Enum.Font.GothamBold
keyButton.TextXAlignment = Enum.TextXAlignment.Center
keyButton.AutoButtonColor = false
keyButton.Parent = mainFrame

local keyButtonCorner = Instance.new("UICorner")
keyButtonCorner.CornerRadius = UDim.new(0, 4)
keyButtonCorner.Parent = keyButton

-- Status Label
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 35)
statusLabel.Position = UDim2.new(0, 0, 0, 210)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "DEACTIVATED"
statusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
statusLabel.TextSize = 16
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextXAlignment = Enum.TextXAlignment.Center
statusLabel.Parent = mainFrame

-- Variables
local isActive = false
local currentKey = "R"

-- Function to update UI
local function updateStatus()
    if isActive then
        statusLabel.Text = "ACTIVATED"
        statusLabel.TextColor3 = Color3.fromRGB(50, 255, 50)
        mainFrame.BorderColor3 = Color3.fromRGB(50, 255, 50)
    else
        statusLabel.Text = "DEACTIVATED"
        statusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
        mainFrame.BorderColor3 = Color3.fromRGB(255, 85, 85)
    end
end

-- Key rebinding
keyButton.MouseButton1Click:Connect(function()
    keyButton.Text = "..."
    local input = UserInputService.InputBegan:Wait()
    if input.UserInputType == Enum.UserInputType.Keyboard then
        currentKey = input.KeyCode.Name
        keyButton.Text = currentKey
    else
        keyButton.Text = currentKey
    end
end)

-- Toggle function
local function toggleScript()
    isActive = not isActive
    updateStatus()
    
    if isActive then
        local powerValue = tonumber(powerBox.Text)
        if powerValue and powerValue >= 10000 and powerValue <= 500000 then
            -- Your speed bypass logic here
            print("Speed Bypass Activated with power: " .. powerValue)
        else
            isActive = false
            updateStatus()
            statusLabel.Text = "INVALID POWER"
            wait(1)
            statusLabel.Text = "DEACTIVATED"
        end
    else
        print("Speed Bypass Deactivated")
    end
end

-- Keybind listener
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode.Name == currentKey then
        toggleScript()
    end
end)

-- Make UI draggable
local dragging = false
local dragStart
local startPos

title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, 
                                       startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Apply glow effect
local glow = Instance.new("ImageLabel")
glow.Size = UDim2.new(1, 20, 1, 20)
glow.Position = UDim2.new(0, -10, 0, -10)
glow.BackgroundTransparency = 1
glow.Image = "rbxassetid://5028857079"
glow.ImageColor3 = Color3.fromRGB(255, 85, 85)
glow.ImageTransparency = 0.7
glow.ZIndex = 0
glow.Parent = mainFrame

-- Animation on show
mainFrame.BackgroundTransparency = 1
mainFrame:TweenSize(UDim2.new(0, 0, 0, 0), "Out", "Quad", 0, true)
wait(0.1)
mainFrame:TweenSize(UDim2.new(0, 350, 0, 250), "Out", "Elastic", 0.5, true)
mainFrame:TweenBackgroundTransparency(0.1, "Out", "Quad", 0.3, true)

-- Your main loader function
local function loadScript()
    -- This is where your actual script logic would go
    -- The UI will automatically show when this loader is executed
    print("K7 Speed Bypass UI Loaded Successfully!")
end

-- Initialize
loadScript()
